describe('Pulling Commits from Github APIs', () => {
  it('should return return stub commits from mock API', () => {

  });

  it('should return commits of testing account from Github API', () => {

  });

  it('should handle without exceptions with repository of no commits', () => {

  });

  it('should handle gracefully with error when parameters are missing', () => {

  });

  it('should not log repeated commits', () => {

  });
});

describe('Pulling Releases from Github APIs', () => {
  it('should return releases from mock API', () => {

  });

  it('should return releases of testing account from Github API', () => {

  });

  it('should handle without exceptions with repository of no releases', () => {

  });

  it('should handle gracefully with error when parameters are missing', () => {

  });

  it('should not log repeated releases', () => {

  });
});
