describe('Pulling Revisions from Google APIs', () => {
  it('should return return stub revisions from mock API', () => {

  });

  it('should return revisions of testing account from Drive API', () => {

  });

  it('should handle without exceptions with drive of no revisions', () => {

  });

  it('should handle gracefully with error when parameters are missing', () => {

  });

  it('should not log repeated revisions', () => {

  });
});

describe('Pulling Files from Google APIs', () => {
  it('should return return stub files from mock API', () => {

  });

  it('should return files of testing account from Drive API', () => {

  });

  it('should handle without exceptions with drive of no files', () => {

  });

  it('should handle gracefully with error when parameters are missing', () => {

  });

  it('should not log repeated files', () => {

  });
});
